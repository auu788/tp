<Window x:Class="GUI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        xmlns:v="clr-namespace:GUI.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <DataTemplate x:Key="ProductsList">
            <v:ProductsList/>
        </DataTemplate>
        <DataTemplate x:Key="ReviewsList">
            <v:ReviewsList/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="250" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ContentControl ContentTemplate="{StaticResource ProductsList}" Grid.Row="0" />
        <ContentControl ContentTemplate="{StaticResource ReviewsList}" Grid.Row="1" />
    </Grid>
</Window>
